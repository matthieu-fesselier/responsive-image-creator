# Responsive Image Creator [WORK IN PROGRESS] 
Tools which helps create responsive images and generate HTML code

## Installation
1. Clone the repository
2. Install the dependencies: `yarn install`
3. Run the app: `yarn start`

